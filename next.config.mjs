/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.dummyjson.com",
      "images.unsplash.com",
      "plus.unsplash.com",
      "images.pexels.com",
      "img.lazcdn.com",
    ],
  },
};
export default nextConfig;
